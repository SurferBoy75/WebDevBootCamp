<%- include('partials/head')%>


    <%- include('partials/navBar')%>

        <h1>Your random number is: <%= randoNum %>
        </h1>

        <% if (randoNum % 2===0) { %>
            <h2>That is an even number!</h2>
            <% } else { %>
                <h2>That is an odd number!</h2>
                <% } %>

                    <h3>
                        That number is: <%= randoNum%2===0 ? 'EVEN' : 'ODD' %>
                    </h3>

                    <%- include('partials/footer')%>